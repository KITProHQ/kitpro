# Debian Go control-plane probe — 2026-09-13

Status: **PASS (local disposable probe)**.

The Go control-plane-shaped probe compiled with embedded HTML/CSS/JavaScript, exposed a server-rendered page, REST/JSON observation route, operation-style `202 Accepted` response, SQLite access, and bounded graceful shutdown. It contains no authentication, catalog, dashboard, or production workflow.

Debian VM execution of the control-plane probe: `NOT RUN`; local execution is not a substitute for package/AppArmor/systemd validation.
